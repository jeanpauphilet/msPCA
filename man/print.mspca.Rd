% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{print.mspca}
\alias{print.mspca}
\alias{print_mspca}
\title{Print an mspca Object}
\usage{
\method{print}{mspca}(x, C = NULL, digits = NULL, ...)

print_mspca(sol_object, C = NULL, digits = NULL)
}
\arguments{
\item{x}{An object of class \code{"mspca"}, as returned by \code{\link[=mspca]{mspca()}}.}

\item{C}{(optional) A numeric matrix (p x p). The covariance or correlation
matrix used when fitting. May be omitted for \code{type = "X"} results.}

\item{digits}{An integer or \code{NULL}. Number of significant digits for
display. When \code{NULL} (the default), \code{getOption("digits")} is used,
so the output respects \code{options(digits = ...)}.}

\item{...}{Further arguments required by the \code{print()} generic; not used by
this method.}
}
\value{
Invisibly returns \code{x}.
}
\description{
S3 print method for objects of class \code{"mspca"} returned by \code{\link[=mspca]{mspca()}}.
Displays the sparse loading matrix (restricted to the union of non-zero
rows) together with the percentage of variance explained and the number of
non-zero loadings per component.
}
\details{
When the model was fit from a covariance/correlation matrix
(\code{type = "Sigma"}), pass that matrix as \code{C} so that per-PC variance figures
can be computed; when it was fit from a raw data matrix (\code{type = "X"}), \code{C}
may be omitted because the figures are stored inside the object.
}
\examples{
TestMat <- cor(mtcars)
res <- mspca(TestMat, r = 2, ks = c(4, 4), verbose = FALSE)
print(res, TestMat)
}
